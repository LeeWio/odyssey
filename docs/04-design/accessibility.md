# 无障碍设计规范

无障碍并不是产品开发完成后的补充工作，而是产品架构的一部分。

所有设计与开发都应默认考虑不同用户的使用能力，而不是在实现完成后再进行修补。

---

## 基本要求

所有核心功能都应满足以下要求：

- 支持键盘完成主要操作与导航。
- 保证清晰且可见的焦点状态（Focus）。
- 提供足够的颜色对比度与信息层级。
- 使用合理的语义化结构组织页面内容。
- 为动画提供「减少动态效果（Reduced Motion）」支持。
- 不依赖颜色作为唯一的信息表达方式。
- 主布局提供「Skip to content」跳过链接，目标为 `#main-content`。

## Skip link

`app/(main)/layout.tsx` 在导航之前放置 Skip to content 链接。链接默认 `sr-only`，在键盘聚焦时可见，并跳转到带 `id="main-content"` 的主内容区域。页面内不要再嵌套第二个 `<main>`。

## Reduced motion hooks

- 首页、导航、页脚等 hydration 敏感表面使用 `hooks/use-reduced-motion-preference`（SSR 默认 reduce）。
- 其它已用 `motion/react` 的 `useReducedMotion` 的局部动画，新增代码优先沿用 SSR-safe hook，避免首屏闪烁。

---

## 动画无障碍

所有动画都必须遵循系统的 **Reduced Motion** 设置。

当用户启用「减少动态效果」后，应遵循以下原则：

- 移除大幅位移、缩放、视差等动画效果。
- 避免滚动驱动或持续循环的动画。
- 使用即时切换或轻量的透明度过渡替代复杂动画。
- 不因关闭动画而影响内容展示、交互反馈或功能完整性。

减少动态效果并不意味着减少功能，而是提供更加舒适的交互体验。

---

## 设计阶段验证

无障碍应在设计阶段完成评估，而不是开发完成后的验收项。

新增页面或功能时，应至少验证以下内容：

- 是否能够仅通过键盘完成操作。
- 焦点顺序是否符合阅读与操作逻辑。
- 是否具有足够的颜色对比度。
- 是否正确使用语义化元素。
- 是否支持 Reduced Motion。
- 是否存在仅依赖颜色、动画或鼠标操作才能完成的交互。

只有通过上述检查后，该功能才可视为符合项目规范。
