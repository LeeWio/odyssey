"use client";

import { Typography } from "@heroui/react";
import { motion } from "motion/react";

/**
 * A hardware-accelerated, motion-enhanced Typography component
 * combining HeroUI's semantic design tokens with Framer Motion capabilities.
 */
export const MotionTypography = motion.create(Typography);
